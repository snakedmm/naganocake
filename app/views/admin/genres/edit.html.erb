<div class="container">
  <h2 class="m-3">ジャンル編集</h2>
  <div class="row">
    <div class="col  m-5">
    <%= form_with model: @genre, url: admin_genre_path(@genre) do |f| %>
　  <div class="form-group">
     <%= f.label :name, 'ジャンル名' %>
     <%= f.text_field :name %>
　　 <%= f.submit '変更を保存',class:"btn btn-success" %>
　  </div>
　  <% end %>
　  </div>
  </div>
</div>
