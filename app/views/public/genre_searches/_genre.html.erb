<table class ="table table-hover table-sm bord">
  <thead style="background-color:#FFDBC9">
    <tr>
      <th>ジャンル検索</th>
    </tr>
  </thead> 
  <tbody>
   <%= form_with url: genre_search_path, method: :get do |f| %>
    <% @genres.each do |genre| %>  
    <tr>
      <td>
      <%= link_to genre.name, genre_search_path(genre_id: genre.id),method: :get ,style:"color:#800000" %>
      </td>
    </tr>
    <% end %>
   <% end %>
  </tbody>
</table>
